import { expect } from 'chai';

import * as actions from '../data/types';
import * as methods from '../data/auth/methods';
import reducer from '../data/auth/reducer';

describe('data/auth', () => {
    it('should accept login request', () => {
    });

});